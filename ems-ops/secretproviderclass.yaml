apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
    name: db-secret
spec:
    provider: aws
    parameters:
        objects: |
            - objectName: "/ems/secret/SPRING_DATASOURCE_PASSWORD"
              objectType: "ssmparameter"
